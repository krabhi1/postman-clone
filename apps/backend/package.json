{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev-2": "npx nodemon -e ts --exec \"npm run compile-run\"",
    "dev": "npx nodemon -e js --exec \"node dist/main.js \"",
    "build": "tsc",
    "compile-run": "tsc && node dist/main.js",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^4.17.21", 
    "@types/node": "^20.11.24",
    "nodemon": "^3.1.3",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}