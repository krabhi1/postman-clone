import { ContentProps } from "../BodyTabItem";

export default function Binary(props: ContentProps) {
  const {
    body: { data, type },
    rawOption,
    option,
  } = props;
  return (
   <div>Binary</div>
  );
}
